project gloglotto ;

lib gloglotto : [ glob source/*.cpp ]
	: <cxxflags>-std=c++11
	: <include>include
	: <linkflags>"-lGL -lGLEW -lglut"
	;

import testing ;

for name in matrix vector {
	unit-test test::$(name) : tests/$(name).cpp tests/tinytest/tinytest.c : <library>gloglotto
		: <cxxflags>"-std=c++11 -Iinclude"
		;
}
